---
description: Draft a pipeline config for a CSV from its profile
argument-hint: [path to the CSV, e.g. data/raw/orders.csv]
---

Use the `new-pipeline` skill. Pass the text below to it as the CSV path.
If that text is empty, ask the user for the CSV path and wait for the answer.

$ARGUMENTS
